@model List<AboutProcess>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml";
}


<div class="container">
    <div class="row">
        <div class="col-lg-12">  <a class="btn btn-primary mb-3" asp-area="admin" asp-controller="AboutProsess" asp-action="Create">Create</a> </div>

        <div class="col-lg-12 mt-5">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Icon</th>
                        <th scope="col">Title</th>
                        <th scope="col">Number</th>
                        <th scope="col">Content</th>
                        <th scope="col"></th>


                    </tr>
                </thead>
                <tbody>
                    @{ int i = 0;
                        foreach (var item in Model)
                        {
                            i++;
                                        <tr>
                                            <th scope="row">@i</th>
                                            <td>@item.Icon</td>
                                            <td>@item.Title</td>
                                            <td>@item.Number</td>
                                            <td>@item.Content</td>
                                            <td>
                                                <a class="btn btn-success" asp-area="admin" asp-controller="AboutProsess" asp-action="Update" asp-route-id="@item.Id">Update</a>
                                                <a class="btn btn-danger" asp-area="admin" asp-controller="AboutProsess" asp-action="Delete" asp-route-id="@item.Id" onclick="return confirm('Are You Sure?')">Delete</a>

                                            </td>
                                        </tr>
                        }
                    }


                </tbody>
            </table>
        </div>
    </div>
</div>



